# Guitar MES Copilot 作業依頼テンプレート

- 作成日: 2026-08-26
- 対象: Guitar Manufacturing Execution System（Guitar MES）
- 用途: 新しいチャットや日々の開発作業で、完成版コードを依頼するための共通テンプレート

---

## 1. 基本運用

作業開始時は、原則として次の3点を共有する。

1. 最新の引き継ぎメモ
2. この作業依頼テンプレート
3. 今回変更する対象ファイルと関連ファイル

基本方針は、差分や部分コードではなく、そのまま既存ファイルと置き換えられる完成版を受け取ることとする。

---

## 2. フル版テンプレート

大きな機能追加、複数ファイル変更、ServiceやControllerを含む修正で使用する。

```text
Guitar MES開発を継続します。

添付したファイルを、以下の要件に従って修正してください。

【今回の作業目的】
ここに今回実現したい内容を書いてください。

例：
production-order-detail.htmlを共通UIへ移行する。
既存の表示項目とネック取付機能は維持する。

【出力方針】
・差分ではなく、修正後のファイル全体を出力してください
・そのままコピーして既存ファイルと置き換えられる状態にしてください
・既存機能、URL、Thymeleaf条件、フォーム送信先を維持してください
・省略記号（...）を使用せず、必要なコードをすべて記載してください
・import、package宣言、DOCTYPE、閉じタグなども省略しないでください
・コンパイルまたは画面表示できる状態を前提にしてください
・変更箇所の解説は必要最小限にしてください
・解説よりも完成版コードの出力を優先してください
・複数ファイルの変更が必要な場合は、変更対象ごとに完成版を出力してください
・変更不要なファイルは出力しないでください

【既存機能の維持】
・現在動作している業務ロジックを削除しないでください
・ControllerのURLを無断で変更しないでください
・Thymeleafのmodel属性名を無断で変更しないでください
・Entity、DTO、Serviceの既存フィールドやメソッドを無断で削除しないでください
・仕様変更が必要な場合は、コード出力前に理由を明示してください
・既存コードから判断できない内容を推測で追加しないでください

【UIデザイン方針】
・白、黒、赤を基調としてください
・現在のGuitar MES共通UIへ統一してください
・可能な限り既存の共通CSSクラスを再利用してください
・PC画面と小画面の両方を考慮してください
・列数の多いテーブルはtable-container内で横スクロールさせてください

使用可能な主な共通クラス：
・page-container
・page-toolbar
・page-toolbar-description
・page-toolbar-actions
・table-container
・data-table
・empty-state
・btn
・btn-primary
・btn-secondary
・btn-outline
・btn-detail
・btn-process-end
・status-badge

【CSSの出力方針】
・既存クラスを再利用できる場合は、新しいCSSを追加しないでください
・追加CSSが必要な場合は、style.cssへ追加するコードを別ブロックで出力してください
・既存CSSと重複する定義を作らないでください
・追加位置の目安も記載してください
・既存style.css全体の出力は、明示的に依頼した場合のみ行ってください

【Javaの出力方針】
・package宣言とimportを含むクラス全体を出力してください
・未使用importを残さないでください
・Controllerに業務ロジックを書きすぎないでください
・業務ルールとトランザクション処理はServiceへ置いてください
・単純なID受け渡しだけのためにDTOを増やさないでください
・複数入力、入力検証、一括処理、複合表示が必要な場合はDTOを使用してください
・例外処理とnull対策を確認してください
・既存の命名規則に合わせてください

【HTMLの出力方針】
・DOCTYPEからhtml終了タグまで、ファイル全体を出力してください
・Thymeleaf式を維持してください
・tbody、table、div、main、body、htmlの閉じ忘れがないか確認してください
・0件時は可能な限りempty-stateを使用してください
・フォームのaction、method、hidden項目を維持してください
・ボタンは可能な限り共通btnクラスを使用してください

【出力順】
1. 修正方針の短い説明
2. 対象ファイルの完成版
3. 必要な場合のみstyle.css追加コード
4. 変更したポイント
5. 動作確認項目

【対象ファイル】
・ここにファイル名を書いてください

【関連ファイル】
・ここにController、Service、Entity、DTO、CSSなどを書いてください

【今回変更しないもの】
・必要があれば、変更対象外を記載してください
```

---

## 3. 短縮版テンプレート

同じチャット内でプロジェクトの前提が共有済みの場合に使用する。

```text
Guitar MES開発です。

添付ファイルを以下の方針で修正してください。

【目的】
ここに作業内容を書いてください。

【出力条件】
・差分ではなく完成版を出力してください
・そのまま既存ファイルと置換可能にしてください
・コードを省略しないでください
・既存機能、URL、Thymeleaf条件、model属性名を維持してください
・複数ファイルの変更が必要なら、各ファイルの完成版を出力してください
・解説は最低限にして、実装を優先してください
・追加CSSはstyle.css用として別に出力してください
・既存の共通UIクラスを優先して再利用してください
・閉じタグ、import、null対策、例外処理を確認してください

【対象ファイル】
○○○

【関連ファイル】
○○○
```

---

## 4. HTML画面専用テンプレート

一覧、詳細、フォーム、工程履歴などのHTML画面を修正する場合に使用する。

```text
Guitar MESのHTML画面を共通UIへ移行してください。

【対象】
production-order-detail.html

【目的】
ここに今回の画面修正内容を書いてください。

【要件】
・差分ではなく、DOCTYPEからhtml終了タグまで完成版を出力してください
・そのままファイルを置き換えられる状態にしてください
・既存の表示項目、Thymeleaf条件、URL、フォーム送信先を維持してください
・page-container、page-toolbar、btn系など既存共通クラスを再利用してください
・0件表示が必要な場合はempty-stateを使用してください
・閉じタグ不足がないか確認してください
・省略記号を使用しないでください
・解説は最低限にしてください

追加CSSが必要な場合は、style.cssへ追記するコードだけを別ブロックで出力してください。
既存CSSと重複する定義は作らないでください。

【関連ファイル】
・Controller
・Service
・Entity
・DTO
・style.css
```

---

## 5. Javaクラス専用テンプレート

Controller、Service、Entity、DTO、RepositoryなどのJavaクラスを修正する場合に使用する。

```text
Guitar MESのJavaクラスを修正してください。

【対象】
ProductionOrderService.java

【目的】
ここに変更内容を書いてください。

【要件】
・差分ではなく、package宣言とimportを含むクラス全体を出力してください
・そのまま既存ファイルと置き換えられる状態にしてください
・既存のpublicメソッドと業務ロジックを維持してください
・未使用importを削除してください
・null対策、例外処理、トランザクション境界を確認してください
・Controllerへ業務ロジックを移動しないでください
・不要なDTOやクラスを増やさないでください
・既存の命名規則に合わせてください
・省略記号を使用しないでください
・解説は最低限にしてください

複数クラスの変更が不可欠な場合は、必要なファイル名を示したうえで、各ファイルの完成版を出力してください。

【関連ファイル】
・ここにController、Service、Entity、DTO、Repositoryなどを書いてください
```

---

## 6. 複数ファイル同時修正テンプレート

CRUD追加、日産計画、個体一括発行、ログイン機能など、複数ファイルへまたがる機能追加で使用する。

```text
Guitar MESの以下の機能を実装してください。

【機能】
ここに実装する機能を書いてください。

例：
Productマスタの編集機能

【対象ファイル】
・ProductViewController.java
・ProductService.java
・product-form.html
・product-detail.html
・style.css

【出力条件】
・差分ではなく、変更が必要な各ファイルの完成版を出力してください
・変更不要なファイルは出力しないでください
・ファイル名を見出しとして明記してください
・そのまま既存ファイルと置き換え可能にしてください
・既存の登録、一覧、詳細機能を維持してください
・URL、model属性名、Thymeleaf条件を無断で変更しないでください
・省略記号を使用しないでください
・追加CSSはstyle.cssへ追記する部分だけを出力してください
・解説は最後に変更点と動作確認項目だけ記載してください

【既存仕様】
・ここに維持すべき業務ルールを書いてください

【今回変更しないもの】
・ここに変更対象外を書いてください
```

---

## 7. レビュー・リファクタリング用テンプレート

既存機能を維持しながら、コード品質、責務、例外処理などを見直す場合に使用する。

```text
Guitar MESの添付ファイルをレビューし、必要なリファクタリングを行ってください。

【対象ファイル】
○○○Service.java

【確認項目】
・責務が適切か
・Controllerに業務ロジックが入っていないか
・Serviceが肥大化していないか
・トランザクション境界が適切か
・null対策があるか
・例外処理が適切か
・重複処理がないか
・命名が既存ルールに合っているか
・不要なDTOやMapperを増やしていないか
・既存機能を壊していないか

【出力条件】
・差分ではなくクラス全体の完成版を出力してください
・package宣言とimportを省略しないでください
・そのまま既存ファイルと置き換え可能にしてください
・既存のpublicメソッドは、明確な理由がない限り維持してください
・仕様変更が必要な場合は、コード出力前に理由を説明してください
・解説は変更点と注意点だけにしてください
```

---

## 8. 最重要の共通指示

依頼文が長くなりすぎる場合でも、最低限次の2文を含める。

```text
差分や部分コードではなく、そのまま既存ファイルと置き換えられる完成版を、コードの省略なしで出力してください。
```

```text
添付ファイルに存在する既存機能、URL、Thymeleaf条件、model属性名は、今回の要件と関係がない限り変更・削除しないでください。
```

---

## 9. 推奨する依頼手順

### 新しいチャットの場合

```text
1. 最新のGuitar MES引き継ぎメモを添付
2. このテンプレートから適切な依頼文を貼り付け
3. 対象ファイルを添付
4. 関連するController、Service、Entity、DTO、CSSを添付
5. 完成版を受け取る
6. ファイルを置換
7. コンパイルまたは画面確認
8. エラーや表示結果を共有
```

### 同じチャットで継続する場合

```text
1. 短縮版テンプレートを貼り付け
2. 作業目的を記入
3. 対象ファイルを添付
4. 完成版を受け取る
5. 動作確認結果を共有
```

---

## 10. 使用上の注意

- 対象ファイルだけで判断できない場合は、関連ファイルも添付する
- ControllerのURL、Serviceのpublicメソッド、HTMLのmodel属性名は特に明記する
- DB変更が必要な場合は、EntityだけでなくSQLまたはマイグレーション方針も依頼する
- 複数ファイルを一度に変更すると確認範囲が広がるため、機能単位で区切る
- 大規模変更では、Entity → Repository → Service → Controller → HTMLの順に確認する
- 完成版を受け取った後も、コンパイル、画面表示、既存導線、入力検証を確認する
- 実績データの物理削除や既存業務ロジックの変更は、明示的な要件がある場合のみ行う

---

## 11. Guitar MES共通開発方針

- ProductionOrder中心の正式フローを維持する
- Assembly時にGuitarを自動生成する
- ProductとBodyMaster / NeckMasterの適合性を維持する
- UIは白、黒、赤を基調とする
- 既存の共通CSSクラスを優先する
- マスタ系は編集・無効化を整備する
- 実績系は原則として物理削除しない
- ProductionOrderは発行前のみ編集可能とする方向で設計する
- 日産計画、個体一括発行、一括工程操作を今後追加する
- 工程別ページと工程内作業を今後追加する
- 差し戻し・再作業を履歴として管理する
- 将来的にADMIN / WORKERのログイン・権限管理を導入する
- DTOは入力検証、一括処理、複合表示、API契約で必要な場合に使用する
- パッケージ構成は当面レイヤー別を維持し、新機能から機能単位構成を検討する
